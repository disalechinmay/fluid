import { PUBLIC_REACT_APP_AUTH0_DOMAIN, PUBLIC_REACT_APP_AUTH0_CLIENT_ID } from '$env/static/public';

const config = {
    domain: PUBLIC_REACT_APP_AUTH0_DOMAIN,
    clientId: PUBLIC_REACT_APP_AUTH0_CLIENT_ID
};

export default config;